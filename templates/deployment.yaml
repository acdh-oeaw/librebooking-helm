apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "librebooking.fullname" . }}
  labels:
    {{- include "librebooking.labels" . | nindent 4 }}
spec:
  replicas: {{ default 1 .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "librebooking.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "librebooking.selectorLabels" . | nindent 8 }}
      annotations:
        # Rollout trigger for external config changes
        config/reload-token: {{ default "v1" .Values.config.reloadToken | quote }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      serviceAccountName: {{ include "librebooking.serviceAccountName" . }}

      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      containers:
        - name: librebooking
          image: "{{ .Values.image.repository }}:{{ default "latest" .Values.image.tag }}"
          imagePullPolicy: {{ default "IfNotPresent" .Values.image.pullPolicy }}

          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}

          ports:
            - name: http
              containerPort: 8080
              protocol: TCP

          env:
            # App-specific settings
            - name: LB_DEFAULT_TIMEZONE
              value: "{{ default "UTC" (.Values.app.defaultTimezone) }}"
            - name: LB_LOGGING_FOLDER
              value: "{{ default "/var/log/librebooking" (.Values.app.loggingFolder) }}"
            - name: LB_LOGGING_LEVEL
              value: "{{ default "INFO" (.Values.app.loggingLevel) }}"
            - name: LB_LOGGING_SQL
              value: "{{ default "false" (.Values.app.loggingSql) }}"
            {{- if .Values.app.appPath }}
            - name: APP_PATH
              value: "{{ .Values.app.appPath }}"
            {{- end }}

            # Installer password (from Secret)
            - name: LB_INSTALL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-install" (include "librebooking.fullname" .)) .Values.app.existingSecretName }}
                  key: {{ default "LB_INSTALL_PASSWORD" .Values.app.existingSecretKey }}

            # Database configuration
            {{- if .Values.externalDatabase.enabled }}
            - name: LB_DATABASE_HOSTSPEC
              value: "{{ .Values.externalDatabase.hostspec }}"
            - name: LB_DATABASE_NAME
              value: "{{ .Values.externalDatabase.name }}"
            - name: LB_DATABASE_USER
              value: "{{ .Values.externalDatabase.user }}"
            - name: LB_DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-db" (include "librebooking.fullname" .)) .Values.externalDatabase.existingSecretName }}
                  key: {{ default "db-password" .Values.externalDatabase.existingSecretKey }}
            {{- else if .Values.mariadb.enabled }}
            - name: LB_DATABASE_HOSTSPEC
              value: {{ printf "%s-mariadb" (include "librebooking.fullname" .) | quote }}
            - name: LB_DATABASE_NAME
              value: "{{ .Values.mariadb.auth.database }}"
            - name: LB_DATABASE_USER
              value: "{{ .Values.mariadb.auth.username }}"
            - name: LB_DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-mariadb" (include "librebooking.fullname" .)) .Values.mariadb.auth.existingSecretName }}
                  key: mysql-password
            {{- end }}

            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}

          {{- $l := default (dict) .Values.livenessProbe -}}
          {{- $r := default (dict) .Values.readinessProbe -}}

          livenessProbe:
            httpGet:
              path: {{ if .Values.app.appPath }}{{ printf "%s/" .Values.app.appPath }}{{ else }}/{{ end }}
              port: http
            initialDelaySeconds: {{ default 30 $l.initialDelaySeconds }}
            periodSeconds: {{ default 15 $l.periodSeconds }}
            failureThreshold: {{ default 6 $l.failureThreshold }}

          readinessProbe:
            httpGet:
              path: {{ if .Values.app.appPath }}{{ printf "%s/" .Values.app.appPath }}{{ else }}/{{ end }}
              port: http
            initialDelaySeconds: {{ default 20 $r.initialDelaySeconds }}
            periodSeconds: {{ default 10 $r.periodSeconds }}
            failureThreshold: {{ default 3 $r.failureThreshold }}

          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}

          volumeMounts:
            # Mount read-only config files from external ConfigMaps/Secrets
            {{- if or .Values.config.bookingConfigMapName .Values.config.bookingSecretName }}
            - name: booking-config
              mountPath: /config/config.php
              subPath: config.php
              readOnly: true
            {{- end }}
            {{- if or .Values.config.ldapConfigMapName .Values.config.ldapSecretName }}
            - name: ldap-config
              mountPath: /config/ldap.config.php
              subPath: ldap.config.php
              readOnly: true
            {{- end }}

            {{- $p := default (dict) .Values.persistence -}}
            {{- $logs := default (dict) $p.logs -}}
            {{- $images := default (dict) $p.uploadsImages -}}
            {{- $res := default (dict) $p.uploadsReservation -}}

            {{- if $logs.enabled }}
            - name: logs
              mountPath: "{{ $logs.mountPath }}"
            {{- end }}
            {{- if $images.enabled }}
            - name: uploads-images
              mountPath: "{{ $images.mountPath }}"
            {{- end }}
            {{- if $res.enabled }}
            - name: uploads-reservation
              mountPath: "{{ $res.mountPath }}"
            {{- end }}

            {{- $fav := default (dict) .Values.favicon -}}
            {{- if $fav.enabled }}
            - name: favicon
              mountPath: "{{ $fav.path }}"
              subPath: "favicon.ico"
              readOnly: true
            {{- end }}

            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}

      volumes:
        # External booking config (ConfigMap or Secret), mounted as /config/config.php
        {{- if or .Values.config.bookingConfigMapName .Values.config.bookingSecretName }}
        - name: booking-config
          {{- if .Values.config.bookingSecretName }}
          secret:
            secretName: {{ .Values.config.bookingSecretName }}
            items:
              - key: config.php
                path: config.php
          {{- else }}
          configMap:
            name: {{ .Values.config.bookingConfigMapName }}
            items:
              - key: config.php
                path: config.php
          {{- end }}
        {{- end }}

        # External LDAP config (ConfigMap or Secret), mounted as /config/ldap.config.php
        {{- if or .Values.config.ldapConfigMapName .Values.config.ldapSecretName }}
        - name: ldap-config
          {{- if .Values.config.ldapSecretName }}
          secret:
            secretName: {{ .Values.config.ldapSecretName }}
            items:
              - key: ldap.config.php
                path: ldap.config.php
          {{- else }}
          configMap:
            name: {{ .Values.config.ldapConfigMapName }}
            items:
              - key: ldap.config.php
                path: ldap.config.php
          {{- end }}
        {{- end }}

        {{- $p := default (dict) .Values.persistence -}}
        {{- $logs := default (dict) $p.logs -}}
        {{- $images := default (dict) $p.uploadsImages -}}
        {{- $res := default (dict) $p.uploadsReservation -}}

        {{- if $logs.enabled }}
        - name: logs
          {{- if $logs.existingClaim }}
          persistentVolumeClaim:
            claimName: {{ $logs.existingClaim }}
          {{- else }}
          persistentVolumeClaim:
            claimName: {{ include "librebooking.fullname" . }}-logs
          {{- end }}
        {{- end }}

        {{- if $images.enabled }}
        - name: uploads-images
          {{- if $images.existingClaim }}
          persistentVolumeClaim:
            claimName: {{ $images.existingClaim }}
          {{- else }}
          persistentVolumeClaim:
            claimName: {{ include "librebooking.fullname" . }}-uploads-images
          {{- end }}
        {{- end }}

        {{- if $res.enabled }}
        - name: uploads-reservation
          {{- if $res.existingClaim }}
          persistentVolumeClaim:
            claimName: {{ $res.existingClaim }}
          {{- else }}
          persistentVolumeClaim:
            claimName: {{ include "librebooking.fullname" . }}-uploads-reservation
          {{- end }}
        {{- end }}

        {{- $fav := default (dict) .Values.favicon -}}
        {{- if $fav.enabled }}
        - name: favicon
          {{- if eq (default "configmap" $fav.type) "secret" }}
          secret:
            secretName: {{ $fav.name }}
          {{- else }}
          configMap:
            name: {{ $fav.name }}
          {{- end }}
        {{- end }}

        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}

      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
